# modularity-loss

This repo contains a short research paper exploring modularity in neural networks via noise-gating, using learned mixing and Gaussian regularization to limit information flow through residual blocks. It is a LaTeX paper source with the manuscript in `noise-gating.tex`.
